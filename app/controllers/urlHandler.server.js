'use strict';

function urlHandler(db) {
/*    
    var urls = db.collection('urls');
    
    this.getShortUrl = function(req, res) {
        urls.findOne({}, {sort:{$natural:-1}}, function(err, test) {
            if(err) {throw err;}
            
            res.json({"url": "test"});
            db.close();
        });
        
    };
    
    this.getRedirectUrl = function(req, res) {
        res.json("test");
        db.close();
    };
*/    
}

module.exports = urlHandler;